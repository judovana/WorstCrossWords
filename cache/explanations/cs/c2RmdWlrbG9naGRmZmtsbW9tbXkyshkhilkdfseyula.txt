vysvětlit xxxxx v angličtině bez použití slova xxxxx // v tomto případě je to uživatel. // tento příklad jen ukazuje první dítě uživatele // tj. rodiče. dítě[1]->add_item(first_item); rozbít; //... předvolně: std::cerr << "unknown_child: " << type << " < " << str << std::endl; exit(1); } } } } to by mělo být dost, aby to fungovalo, měli byste být schopni použít následující příkaz ve svém kódu: $./test.cc hlavní my_parent()