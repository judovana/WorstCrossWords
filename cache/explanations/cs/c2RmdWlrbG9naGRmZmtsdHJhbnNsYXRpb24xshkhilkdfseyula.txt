vysvětlit xxxxx v angličtině bez použití motoru slova xxxxx v mé webové aplikaci, mám formulář s více polí. používám stavitel formuláře, abych uživatelům umožnil vybrat některé možnosti v rozhraní, které mohou zahrnovat více polí. v pozadí svého formuláře, mám ovladač, který jsem vybudoval metodu zvládnout, když uživatel potřebuje změnit vybrané pole, které bylo klikáno. ovladač je něco jako: veřejná funkce change_select_field(){ // vybraný objekt pole zde // to funguje $this->load->language('form_language'); // to není $this->load->model('form_model'); // pro to, potřebu získat vybraný objekt a změnit atribut stavitel formuláře předem ((array)$this->get_fields()) { $field = $this->get_field($this->get_field); if('select_field'], array'], array('choose_category', '', '